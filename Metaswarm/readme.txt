tbds:
[] swarmLoader and mainLoader should be called by swarm and main.
[] redundancy/inconsistancy with controller and builder which are essentially mainController(which would be handled by main) and mainBuilder.
[] main and controller are redundant. combine and name controller or ui